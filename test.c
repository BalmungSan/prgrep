#include <stdio.h>

int test (char* text, char* pattern) {
   int result = -1;

   char* test = text;

   return result;
}

int main (int argc, char** argv) {
   int r = test(argv[1], argv[2]); 
   return 0;
}
